interface Flight {
  id?: Int16Array;
  flight_number: string;
  flight_date: string;
  aircraft_id: string;

  // origin: string;
  // destination: string;
  // departureTime: string;
  // arrivalTime: string;
}